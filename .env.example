# Example .env file for WordPress MCP Server

# The base URL for your WordPress REST API
# Example: https://your-wordpress-site.com/wp-json/wp/v2
WORDPRESS_API_URL=YOUR_WORDPRESS_SITE_URL/wp-json/wp/v2

# Optional: Basic Authentication credentials
# Use Application Passwords plugin in WordPress for better security
# Leave blank if your API doesn't require authentication
WORDPRESS_AUTH_USER=your_wordpress_username
WORDPRESS_AUTH_PASS=your_application_password

# Optional: Port for the MCP server to run on (defaults to 3000 if not set)
# PORT=3001
