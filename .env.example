# Example .env file for WordPress MCP Server

# Mode to start the MCP server (stdio or http -default-)
#MCP_TRANSPORT=stdio

# Bearer token for authentication of protected methods when using HTTP transport (MCP_TRANSPORT=http)
# Only used for deprecated SSE transport for the new Streamable HTTP API, Oauth2 has to be implemented (see branch feature/oauth2)
#MCP_AUTH_TOKEN=your_secure_token_here

# The base URL for your WordPress REST API
# Example: https://your-wordpress-site.com/wp-json/wp/v2
WORDPRESS_API_URL=YOUR_WORDPRESS_SITE_URL/wp-json/wp/v2

# Wordpress Basic Authentication credentials
# Use Application Passwords in WordPress (https://make.wordpress.org/core/2020/11/05/application-passwords-integration-guide/)
WORDPRESS_AUTH_USER=your_wordpress_username
WORDPRESS_AUTH_PASS=your_application_password

# Server configuration
# Port for the MCP server to run on (defaults to 3000 if not set)
#PORT=3000

# Security settings
# Maximum allowed file size for uploads in MB (default: 50)
MAX_FILE_SIZE_MB=50

# Comma-separated list of allowed file extensions for uploads
ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,webp

# Rate limiting (requests per minute per IP, default: 60)
RATE_LIMIT=60
